#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_mockjs@1.1.0@mockjs/bin/random" "$@"
  ret=$?
else 
  node  "$basedir/../_mockjs@1.1.0@mockjs/bin/random" "$@"
  ret=$?
fi
exit $ret
