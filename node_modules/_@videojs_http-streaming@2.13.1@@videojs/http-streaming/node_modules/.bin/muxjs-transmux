#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_mux.js@6.0.1@mux.js/bin/transmux.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_mux.js@6.0.1@mux.js/bin/transmux.js" "$@"
  ret=$?
fi
exit $ret
