#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_mpd-parser@0.21.0@mpd-parser/bin/parse.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_mpd-parser@0.21.0@mpd-parser/bin/parse.js" "$@"
  ret=$?
fi
exit $ret
